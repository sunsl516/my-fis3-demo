{
  "name": "my-fis3-demo",
  "version": "1.0.0",
  "description": "my-fis3-demo",
  "main": "fis-conf.js",
  "scripts": {
    "dev": "fis3 server start -p 8000 && fis3 release -wL",
    "build": "rm -rf dist && fis3 release prod -d ./dist && rm -rf ./dist/sass",
    "test": "fis3 release -d ./dist && fis3 server start --root ./dist && rm -rf dist  && fis3 release -d ./dist -wL"
  },
  "author": "louis",
  "license": "ISC",
  "devDependencies": {
    "fis-optimizer-html-minifier": "0.0.9",
    "fis-parser-babel-5.x": "^1.4.0",
    "fis-parser-handlebars-3.x": "0.0.1",
    "fis-parser-node-sass": "^1.0.1"
  }
}